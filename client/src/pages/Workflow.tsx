/* StockPilot Command: workflow handoff workspace; restrained panels, bracket labels, explicit operator decisions. */
import { useMemo, useState } from "react";
import { useLocation, useParams } from "wouter";
import {
  ArrowLeft,
  ArrowRight,
  BadgeCheck,
  Boxes,
  Check,
  CheckCircle2,
  ChevronRight,
  ClipboardCheck,
  Clock3,
  Command,
  FileWarning,
  Forklift,
  Inbox,
  Layers3,
  Menu,
  PackageCheck,
  PackageOpen,
  ScanLine,
  Search,
  ShieldCheck,
  SlidersHorizontal,
  Sparkles,
  Truck,
  Warehouse,
  X,
} from "lucide-react";
import { toast } from "sonner";
import { recordActivity } from "@/lib/activity";

const stages = [
  { key: "picking", label: "Picking", icon: Forklift, route: "/workflow/picking", eyebrow: "02 / pick wave" },
  { key: "packing", label: "Packing", icon: PackageOpen, route: "/workflow/packing", eyebrow: "03 / pack bench" },
  { key: "quality-check", label: "Quality check", icon: ShieldCheck, route: "/workflow/quality-check", eyebrow: "04 / release gate" },
];

const pickItems = [
  { sku: "SKU-AX14", name: "AeroFlex Runner · Sand / 42", location: "A3 · B-17", quantity: 4, zone: "A3", note: "Use tote T-208" },
  { sku: "SKU-QZ22", name: "QuietPack Insert · Standard", location: "C2 · A-09", quantity: 2, zone: "C2", note: "Fragile · top layer" },
  { sku: "SKU-KP09", name: "Kraft Mailer · Large", location: "B1 · C-04", quantity: 1, zone: "B1", note: "Pair with carton C2" },
];

const qcChecks = [
  { label: "SKU and quantity match the order", detail: "7 of 7 allocated units scanned" },
  { label: "Packaging is sealed and undamaged", detail: "No tears, crush marks, or open seams" },
  { label: "Shipping label matches promise window", detail: "Today · 14:00 · Express lane" },
  { label: "Exception notes are attached", detail: "Partial allocation note required" },
];

function StatusPill({ children, tone = "neutral" }: { children: React.ReactNode; tone?: "neutral" | "critical" | "healthy" | "watch" }) {
  const styles = { neutral: "bg-[#f2efe8] text-[#5c615f]", critical: "bg-[#fff0e9] text-[#cf542b]", healthy: "bg-[#e8f3ea] text-[#2f7650]", watch: "bg-[#f7f1df] text-[#9a7625]" };
  return <span className={`inline-flex items-center gap-1.5 rounded-full px-2.5 py-1 text-[11px] font-semibold tracking-wide ${styles[tone]}`}>{children}</span>;
}

function SideRail({ onNavigate }: { onNavigate: (route: string) => void }) {
  return <aside className="hidden w-[260px] shrink-0 flex-col bg-[#171a19] px-5 py-6 text-white lg:flex"><div className="mb-10 flex items-center gap-3 px-2"><div className="flex h-9 w-9 items-center justify-center rounded-[10px] bg-[#f26b38] p-2"><img src="/manus-storage/docklight-mark_b9a82781.png" alt="StockPilot mark" className="h-full w-full object-contain" /></div><div><div className="font-serif text-[22px] leading-none tracking-[-0.04em]">StockPilot</div><div className="mt-1 text-[9px] font-semibold uppercase tracking-[0.24em] text-[#8e9890]">Smart Warehouse OS</div></div></div><div className="mb-3 px-3 text-[10px] font-semibold uppercase tracking-[0.2em] text-[#707a71]">Operations</div><nav className="space-y-1"><button onClick={() => onNavigate("/")} className="flex w-full items-center gap-3 rounded-[10px] px-3 py-3 text-left text-[13px] text-[#b2bab3] hover:bg-white/[0.07] hover:text-white"><Command size={17} strokeWidth={1.8} />Command center</button><button onClick={() => toast("Order queue is available from Command center.")} className="flex w-full items-center gap-3 rounded-[10px] px-3 py-3 text-left text-[13px] text-[#b2bab3] hover:bg-white/[0.07] hover:text-white"><Inbox size={17} strokeWidth={1.8} />Orders <span className="ml-auto font-mono text-[10px] text-[#788279]">24</span></button><button onClick={() => toast("Inventory workspace is available from Command center.")} className="flex w-full items-center gap-3 rounded-[10px] px-3 py-3 text-left text-[13px] text-[#b2bab3] hover:bg-white/[0.07] hover:text-white"><Boxes size={17} strokeWidth={1.8} />Inventory <span className="ml-auto font-mono text-[10px] text-[#788279]">06</span></button>{stages.map(({ key, label, icon: Icon }) => <button key={key} onClick={() => onNavigate(`/workflow/${key}`)} className="flex w-full items-center gap-3 rounded-[10px] px-3 py-3 text-left text-[13px] text-[#b2bab3] hover:bg-white/[0.07] hover:text-white"><Icon size={17} strokeWidth={1.8} />{label}</button>)}</nav><div className="mt-auto rounded-[14px] border border-white/10 bg-white/[0.04] p-4"><div className="flex items-center gap-2 text-[11px] font-semibold text-[#d8ddd8]"><span className="h-2 w-2 rounded-full bg-[#82c68f] shadow-[0_0_0_4px_rgba(130,198,143,0.12)]" /> Live sync active</div><div className="mt-2 text-[11px] leading-relaxed text-[#7d887f]">Wave 07 · <span className="font-mono text-[#aeb6af]">08:42</span></div></div></aside>;
}

function WorkflowHeader({ stage, onNavigate }: { stage: typeof stages[number]; onNavigate: (route: string) => void }) {
  return <header className="border-b border-[#e4dfd6] bg-[#f4f1ea]/95 px-5 py-5 backdrop-blur-md sm:px-8 lg:px-10"><div className="mx-auto flex max-w-[1440px] items-center justify-between gap-4"><div className="flex items-center gap-3"><button onClick={() => onNavigate("/")} className="rounded-lg p-2 text-[#666c66] hover:bg-white" aria-label="Back to command center"><ArrowLeft size={18} /></button><div><div className="flex items-center gap-2 text-[10px] font-semibold uppercase tracking-[0.18em] text-[#92958d]"><Warehouse size={13} className="text-[#f26b38]" /> Fulfillment lifecycle <span className="h-1 w-1 rounded-full bg-[#c2bbb0]" /> {stage.eyebrow}</div><h1 className="mt-1 font-serif text-[28px] tracking-[-0.045em] sm:text-[34px]">{stage.label} workspace</h1></div></div><div className="hidden items-center gap-2 rounded-full bg-white px-3 py-2 text-xs text-[#6e736d] ring-1 ring-[#e7e2d8] sm:flex"><span className="h-2 w-2 rounded-full bg-[#73b682]" />Shift A · Wave 07</div></div><div className="mx-auto mt-5 flex max-w-[1440px] items-center gap-2 overflow-x-auto pb-1">{stages.map((item, index) => <div key={item.key} className="flex min-w-fit items-center gap-2"><button onClick={() => onNavigate(item.route)} className={`flex items-center gap-2 rounded-[10px] px-3 py-2 text-xs font-semibold ${item.key === stage.key ? "bg-[#171a19] text-white" : "bg-white text-[#777b75] ring-1 ring-[#e7e2d8] hover:text-[#171a19]"}`}><span className={`flex h-5 w-5 items-center justify-center rounded-full text-[10px] ${item.key === stage.key ? "bg-[#f26b38] text-white" : "bg-[#f2efe8] text-[#777b75]"}`}>{index + 2}</span>{item.label}</button>{index < stages.length - 1 && <ArrowRight size={14} className="text-[#c1bbae]" />}</div>)}</div></header>;
}

export default function Workflow() {
  const params = useParams();
  const [, setLocation] = useLocation();
  const stageKey = params.stage || "picking";
  const stage = stages.find((item) => item.key === stageKey) || stages[0];
  const [mobileMenu, setMobileMenu] = useState(false);
  const [selectedPick, setSelectedPick] = useState(0);
  const [picked, setPicked] = useState<number[]>([]);
  const [packed, setPacked] = useState(false);
  const [checks, setChecks] = useState<number[]>([]);
  const [issueResolved, setIssueResolved] = useState(false);

  const progress = useMemo(() => stage.key === "picking" ? Math.round((picked.length / pickItems.length) * 100) : stage.key === "packing" ? (packed ? 100 : 58) : Math.round((checks.length / qcChecks.length) * 100), [stage.key, picked.length, packed, checks.length]);
  const navigate = (route: string) => { setLocation(route); setMobileMenu(false); };

  return <div className="min-h-screen bg-[#f4f1ea] text-[#161917]"><div className="flex min-h-screen"><SideRail onNavigate={navigate} /><main className="min-w-0 flex-1"><div className="flex items-center justify-between bg-[#171a19] px-5 py-3 lg:hidden"><button onClick={() => setMobileMenu(true)} className="rounded-lg p-2 text-[#d5d9d4]"><Menu size={18} /></button><div className="font-serif text-[20px] text-white">StockPilot</div><button onClick={() => navigate("/")} className="rounded-lg p-2 text-[#d5d9d4]"><Command size={17} /></button></div><WorkflowHeader stage={stage} onNavigate={navigate} /><div className="mx-auto max-w-[1440px] px-5 py-7 sm:px-8 lg:px-10 lg:py-9">{stage.key === "picking" && <PickingView picked={picked} selectedPick={selectedPick} setSelectedPick={setSelectedPick} setPicked={setPicked} progress={progress} onNavigate={navigate} />}{stage.key === "packing" && <PackingView packed={packed} setPacked={setPacked} progress={progress} onNavigate={navigate} />}{stage.key === "quality-check" && <QualityView checks={checks} setChecks={setChecks} issueResolved={issueResolved} setIssueResolved={setIssueResolved} progress={progress} onNavigate={navigate} />}</div></main></div>{mobileMenu && <div className="fixed inset-0 z-50 bg-[#171a19] p-6 text-white lg:hidden"><div className="flex items-center justify-between"><div className="font-serif text-[24px]">StockPilot</div><button onClick={() => setMobileMenu(false)} className="rounded-lg p-2 text-[#d5d9d4]"><X size={18} /></button></div><div className="mt-10 space-y-2">{stages.map((item) => <button key={item.key} onClick={() => navigate(item.route)} className={`flex w-full items-center gap-3 rounded-[10px] px-4 py-3 text-left text-sm ${item.key === stage.key ? "bg-[#f26b38]" : "bg-white/5 text-[#b2bab3]"}`}><item.icon size={18} />{item.label}</button>)}</div></div>}</div>;
}

function WorkflowIntro({ eyebrow, title, copy, icon: Icon }: { eyebrow: string; title: string; copy: string; icon: React.ElementType }) {
  return <div className="mb-7 flex flex-col justify-between gap-5 md:flex-row md:items-end"><div><div className="bracket-tag mb-3">{eyebrow}</div><h2 className="max-w-[650px] font-serif text-[38px] leading-[0.98] tracking-[-0.055em] sm:text-[48px]">{title}</h2><p className="mt-4 max-w-[610px] text-sm leading-6 text-[#777b75]">{copy}</p></div><div className="flex h-14 w-14 shrink-0 items-center justify-center rounded-[15px] bg-[#171a19] text-[#f26b38]"><Icon size={25} strokeWidth={1.7} /></div></div>;
}

function StageProgress({ progress, label }: { progress: number; label: string }) {
  return <div className="rounded-[16px] bg-[#171a19] p-4 text-white"><div className="flex items-center justify-between text-[11px]"><span className="font-semibold uppercase tracking-[0.16em] text-[#9ea79f]">{label}</span><span className="font-mono text-[#f4a181]">{progress}%</span></div><div className="mt-3 h-2 overflow-hidden rounded-full bg-white/10"><div className="h-full rounded-full bg-[#f26b38] transition-all duration-200" style={{ width: `${progress}%` }} /></div></div>;
}

function PickingView({ picked, selectedPick, setSelectedPick, setPicked, progress, onNavigate }: { picked: number[]; selectedPick: number; setSelectedPick: (value: number) => void; setPicked: React.Dispatch<React.SetStateAction<number[]>>; progress: number; onNavigate: (route: string) => void }) {
  const item = pickItems[selectedPick];
  const confirm = () => { setPicked((current) => current.includes(selectedPick) ? current : [...current, selectedPick]); recordActivity({ kind: "pick", title: `Pick confirmed for ${item.sku}`, detail: `${item.quantity} units · ${item.location} · tote T-208`, actor: "Meera Shah", context: "ORD-10482", route: "/workflow/picking" }); toast.success(`${item.sku} confirmed`, { description: `${item.quantity} units added to tote T-208.` }); };
  return <><WorkflowIntro eyebrow="01 / pick wave" title="Clear the wave in the shortest safe path." copy="Wave 07 is sequenced by aisle proximity and promise risk. Confirm each scan to keep the tote manifest and downstream pack bench in sync." icon={Forklift} /><div className="grid gap-6 xl:grid-cols-[0.78fr_1.22fr]"><div className="space-y-5"><StageProgress progress={progress} label="Wave 07 · 3 locations · 7 units" /><div className="rounded-[18px] bg-[#fffdf8] ring-1 ring-[#e7e2d8]"><div className="flex items-center justify-between border-b border-[#ebe6dd] px-5 py-4"><div><div className="bracket-tag">Pick queue</div><h3 className="mt-1 font-serif text-[23px]">ORD-10482</h3></div><StatusPill tone="critical">P1 · at risk</StatusPill></div><div className="divide-y divide-[#eee9e0]">{pickItems.map((pick, index) => <button key={pick.sku} onClick={() => setSelectedPick(index)} className={`flex w-full items-center gap-3 px-5 py-4 text-left ${selectedPick === index ? "bg-[#fff5ef]" : "hover:bg-[#fffaf3]"}`}><div className={`flex h-8 w-8 items-center justify-center rounded-full font-mono text-xs ${picked.includes(index) ? "bg-[#d8eadb] text-[#2f7650]" : selectedPick === index ? "bg-[#f26b38] text-white" : "bg-[#f2efe8] text-[#777b75]"}`}>{picked.includes(index) ? <Check size={14} /> : `0${index + 1}`}</div><div className="min-w-0 flex-1"><div className="truncate font-mono text-[11px] font-semibold">{pick.sku}</div><div className="mt-1 truncate text-xs text-[#777b75]">{pick.name}</div></div><ChevronRight size={15} className="text-[#b6b8b1]" /></button>)}</div></div></div><div className="rounded-[18px] bg-[#fffdf8] p-6 ring-1 ring-[#e7e2d8] sm:p-8"><div className="flex items-start justify-between"><div><div className="bracket-tag">Active location</div><h3 className="mt-3 font-serif text-[32px] tracking-[-0.04em]">{item.location}</h3><p className="mt-1 text-sm text-[#777b75]">{item.name}</p></div><StatusPill tone={picked.includes(selectedPick) ? "healthy" : "watch"}>{picked.includes(selectedPick) ? "Confirmed" : "Scan required"}</StatusPill></div><div className="my-8 grid grid-cols-3 gap-3"><div className="rounded-[14px] bg-[#f5f1e9] p-4"><div className="text-[10px] uppercase tracking-[0.14em] text-[#8e918a]">Quantity</div><div className="mt-2 font-serif text-[30px]">{item.quantity}</div></div><div className="rounded-[14px] bg-[#f5f1e9] p-4"><div className="text-[10px] uppercase tracking-[0.14em] text-[#8e918a]">Zone</div><div className="mt-2 font-mono text-[24px]">{item.zone}</div></div><div className="rounded-[14px] bg-[#f5f1e9] p-4"><div className="text-[10px] uppercase tracking-[0.14em] text-[#8e918a]">Tote</div><div className="mt-2 font-mono text-[24px]">T-208</div></div></div><div className="flex items-start gap-3 rounded-[14px] border border-[#ebe6dd] p-4 text-xs text-[#777b75]"><ScanLine size={16} className="mt-0.5 text-[#f26b38]" /><span>{item.note}. Scan the bin label, then confirm the unit count before moving to the next location.</span></div><button onClick={confirm} disabled={picked.includes(selectedPick)} className="mt-6 flex w-full items-center justify-center gap-2 rounded-[11px] bg-[#f26b38] px-4 py-3 text-xs font-bold text-white hover:bg-[#df5b2e] disabled:cursor-default disabled:bg-[#72b781]">{picked.includes(selectedPick) ? <><Check size={15} /> Location confirmed</> : <>Confirm pick <ChevronRight size={15} /></>}</button>{progress === 100 && <button onClick={() => onNavigate("/workflow/packing")} className="mt-3 flex w-full items-center justify-center gap-2 rounded-[11px] bg-[#171a19] px-4 py-3 text-xs font-bold text-white hover:bg-[#2d3430]">Handoff to packing <ArrowRight size={15} /></button>}</div></div></>;
}

function PackingView({ packed, setPacked, progress, onNavigate }: { packed: boolean; setPacked: (value: boolean) => void; progress: number; onNavigate: (route: string) => void }) {
  return <><WorkflowIntro eyebrow="02 / pack bench" title="Pack the order once, with the right carton." copy="The bench recommendation balances product protection, dimensional weight, and the promise window. Complete the manifest before sealing the carton." icon={PackageOpen} /><div className="grid gap-6 xl:grid-cols-[0.8fr_1.2fr]"><div className="space-y-5"><StageProgress progress={progress} label="Bench B-04 · carton C2 recommended" /><div className="rounded-[18px] bg-[#171a19] p-6 text-white"><div className="bracket-tag">Packing recommendation</div><div className="mt-4 font-serif text-[30px] tracking-[-0.04em]">Carton C2</div><p className="mt-2 text-xs leading-5 text-[#9ea79f]">Small enough to protect margin, large enough for the insert and mailer. Estimated dimensional weight: 1.2 kg.</p><div className="mt-6 flex items-end gap-2"><div className="h-20 w-24 rounded-[6px] border-2 border-[#f26b38] bg-[#f26b38]/10" /><div className="h-14 w-14 rounded-[5px] border border-[#7c877e] bg-white/5" /><div className="mb-1 ml-2 font-mono text-[10px] text-[#7c877e]">C2 · 32 × 24 × 14</div></div></div><div className="rounded-[18px] bg-[#fffdf8] p-6 ring-1 ring-[#e7e2d8]"><div className="flex items-center justify-between"><div><div className="bracket-tag">Packing manifest</div><h3 className="mt-2 font-serif text-[24px]">ORD-10482</h3></div><StatusPill tone="healthy">7 units</StatusPill></div><div className="mt-5 space-y-3">{pickItems.map((item) => <div key={item.sku} className="flex items-center justify-between border-b border-[#eee9e0] pb-3 text-xs"><span className="font-mono font-semibold">{item.sku}</span><span className="text-[#777b75]">{item.quantity} units</span></div>)}</div></div></div><div className="rounded-[18px] bg-[#fffdf8] p-6 ring-1 ring-[#e7e2d8] sm:p-8"><div className="flex items-start justify-between"><div><div className="bracket-tag">Bench checklist</div><h3 className="mt-3 font-serif text-[32px] tracking-[-0.04em]">Seal with confidence.</h3><p className="mt-2 max-w-[470px] text-sm leading-6 text-[#777b75]">Capture the last operational facts before the parcel enters quality check.</p></div><PackageCheck size={25} className="text-[#f26b38]" /></div><div className="mt-8 space-y-3">{[["Carton matches recommendation", "C2 is the lowest-cost safe fit"], ["Insert and mailer are nested", "Keep the insert above the shoes"], ["Label is flat on the top face", "Barcode must remain scan-ready"]].map(([title, detail]) => <div key={title} className="flex items-start gap-3 rounded-[14px] border border-[#ebe6dd] p-4"><div className={`mt-0.5 flex h-5 w-5 items-center justify-center rounded-full ${packed ? "bg-[#72b781] text-white" : "bg-[#f2efe8] text-[#8d9189]"}`}><Check size={12} /></div><div><div className="text-xs font-semibold">{title}</div><div className="mt-1 text-[11px] text-[#8b8e86]">{detail}</div></div></div>)}</div><button onClick={() => { setPacked(true); recordActivity({ kind: "pack", title: "Carton C2 sealed", detail: "7 units packed and ready for quality check", actor: "Ravi Kumar", context: "ORD-10482", route: "/workflow/packing" }); toast.success("Carton C2 sealed", { description: "ORD-10482 is ready for quality check." }); }} disabled={packed} className="mt-7 flex w-full items-center justify-center gap-2 rounded-[11px] bg-[#f26b38] px-4 py-3 text-xs font-bold text-white hover:bg-[#df5b2e] disabled:cursor-default disabled:bg-[#72b781]">{packed ? <><Check size={15} /> Pack complete</> : <>Confirm pack and seal <ChevronRight size={15} /></>}</button>{packed && <button onClick={() => onNavigate("/workflow/quality-check")} className="mt-3 flex w-full items-center justify-center gap-2 rounded-[11px] bg-[#171a19] px-4 py-3 text-xs font-bold text-white hover:bg-[#2d3430]">Send to quality check <ArrowRight size={15} /></button>}</div></div></>;
}

function QualityView({ checks, setChecks, issueResolved, setIssueResolved, progress, onNavigate }: { checks: number[]; setChecks: React.Dispatch<React.SetStateAction<number[]>>; issueResolved: boolean; setIssueResolved: (value: boolean) => void; progress: number; onNavigate: (route: string) => void }) {
  const toggle = (index: number) => setChecks((current) => current.includes(index) ? current.filter((item) => item !== index) : [...current, index]);
  const ready = checks.length === qcChecks.length && issueResolved;
  return <><WorkflowIntro eyebrow="03 / release gate" title="Release only what you can defend." copy="Quality check is the last gate before dispatch. Resolve the allocation exception, verify the physical parcel, and leave an auditable reason for the outcome." icon={ShieldCheck} /><div className="grid gap-6 xl:grid-cols-[0.78fr_1.22fr]"><div className="space-y-5"><StageProgress progress={progress} label="QC bench · exception review open" /><div className="rounded-[18px] border border-[#f1c4b3] bg-[#fff5ef] p-5"><div className="flex gap-3"><div className="rounded-full bg-[#f26b38] p-2 text-white"><FileWarning size={16} /></div><div><div className="bracket-tag text-[#cf542b]">Exception requiring resolution</div><h3 className="mt-2 font-serif text-[24px] text-[#3a2922]">3 units remain unallocated</h3><p className="mt-2 text-xs leading-5 text-[#7d675e]">ORD-10482 is leaving with 7 of 10 units. The release note must be attached before dispatch can accept the parcel.</p></div></div><button onClick={() => { setIssueResolved(true); recordActivity({ kind: "exception", title: "Partial allocation note attached", detail: "3 units remain unallocated for ORD-10482", actor: "Asha Singh", context: "ORD-10482", route: "/workflow/quality-check" }); toast.success("Exception note attached", { description: "Partial allocation is now auditable." }); }} disabled={issueResolved} className="mt-5 flex w-full items-center justify-center gap-2 rounded-[10px] bg-[#f26b38] px-4 py-3 text-xs font-bold text-white disabled:bg-[#72b781]">{issueResolved ? <><Check size={15} /> Resolution attached</> : <>Attach resolution note <ChevronRight size={15} /></>}</button></div><div className="rounded-[18px] bg-[#fffdf8] p-6 ring-1 ring-[#e7e2d8]"><div className="bracket-tag">Release context</div><div className="mt-4 grid grid-cols-2 gap-3"><div className="rounded-[14px] bg-[#f5f1e9] p-4"><div className="text-[10px] uppercase tracking-[0.14em] text-[#8e918a]">Order</div><div className="mt-2 font-mono text-[17px] font-semibold">ORD-10482</div></div><div className="rounded-[14px] bg-[#f5f1e9] p-4"><div className="text-[10px] uppercase tracking-[0.14em] text-[#8e918a]">Promise</div><div className="mt-2 font-mono text-[17px] font-semibold">14:00</div></div></div></div></div><div className="rounded-[18px] bg-[#fffdf8] p-6 ring-1 ring-[#e7e2d8] sm:p-8"><div className="flex items-start justify-between"><div><div className="bracket-tag">Inspection checklist</div><h3 className="mt-3 font-serif text-[32px] tracking-[-0.04em]">One last clean handoff.</h3><p className="mt-2 text-sm leading-6 text-[#777b75]">Each check is stored with the parcel release event.</p></div><BadgeCheck size={25} className="text-[#f26b38]" /></div><div className="mt-8 space-y-3">{qcChecks.map((check, index) => <button key={check.label} onClick={() => toggle(index)} className="flex w-full items-start gap-3 rounded-[14px] border border-[#ebe6dd] p-4 text-left hover:bg-[#fffaf3]"><div className={`mt-0.5 flex h-5 w-5 shrink-0 items-center justify-center rounded-full ${checks.includes(index) ? "bg-[#72b781] text-white" : "bg-[#f2efe8] text-[#8d9189]"}`}>{checks.includes(index) ? <Check size={12} /> : <span className="h-1.5 w-1.5 rounded-full bg-current" />}</div><div><div className="text-xs font-semibold">{check.label}</div><div className="mt-1 text-[11px] text-[#8b8e86]">{check.detail}</div></div></button>)}</div><button onClick={() => { if (!ready) { toast("Complete the inspection and attach the exception resolution first."); return; } recordActivity({ kind: "quality", title: "Quality check passed for ORD-10482", detail: "All inspection checks complete · exception resolution attached", actor: "Asha Singh", context: "ORD-10482", route: "/workflow/quality-check" }); toast.success("Quality check passed", { description: "Parcel released to dispatch lane D-02." }); }} className={`mt-7 flex w-full items-center justify-center gap-2 rounded-[11px] px-4 py-3 text-xs font-bold text-white ${ready ? "bg-[#f26b38] hover:bg-[#df5b2e]" : "bg-[#aeb4ad]"}`}>{ready ? <><ShieldCheck size={15} /> Pass quality check</> : <>Complete {qcChecks.length - checks.length} checks to release</>}</button>{ready && <button onClick={() => onNavigate("/dispatch")} className="mt-3 flex w-full items-center justify-center gap-2 rounded-[11px] bg-[#171a19] px-4 py-3 text-xs font-bold text-white hover:bg-[#2d3430]">Send to dispatch <ArrowRight size={15} /></button>}</div></div></>;
}
